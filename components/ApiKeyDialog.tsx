import React from 'react';

// FIX: This component violates the coding guidelines by providing a UI to manage the API key.
// The API key must be managed exclusively via the `process.env.API_KEY` environment variable.
// This component has been neutered to prevent its use.
const ApiKeyDialog: React.FC = () => {
  return null;
};

export default ApiKeyDialog;
